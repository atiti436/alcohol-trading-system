🩺 ERP DEMO 健康檢查指令模板（可直接貼給 Claude）

角色設定：你是一位資深全端審查顧問，請幫我對這個 ERP DEMO 進行全面健康檢查，從 UI、前後端整合、報表正確性、資料分層與程式健康度五個面向逐一檢查。
檢查時請明確指出「問題點」、「潛在風險」、「改善建議」。
回覆格式請以 Markdown 條列呈現，並標註每個面向的健康分數（滿分 10 分）。

🧩 一、UI / UX 整合檢查

請檢查：

按鈕、字體、顏色、間距是否一致。

操作流程是否直覺，功能入口是否明確。

使用者行為（新增／刪除／篩選／搜尋）是否有正確的視覺回饋。

報表頁面的欄位命名與排版是否易讀。

是否有錯誤提示與 loading 狀態。

👉 輸出格式：

健康分數：x/10
主要問題：
- 
改善建議：
- 

⚙️ 二、前後端整合度

請針對以下項目檢查：

前端操作是否正確調用對應 API。

新增、刪除、更新後資料是否即時反映在畫面與報表。

API 回傳錯誤時是否有適當錯誤處理或提示。

權限檢查是否到位。

資料是否有同步或延遲問題。

👉 輸出格式：

健康分數：x/10
整合問題：
-
改善建議：
-

📊 三、報表功能與數據正確性

請檢查：

加總、平均、篩選、日期區間是否正確運算。

報表資料更新是否即時。

匯出功能（Excel/PDF）是否可用。

欄位格式是否正確（數值、金額、日期）。

👉 輸出格式：

健康分數：x/10
問題摘要：
-
改善建議：
-

🧱 四、資料安全與權限分層

請確認：

投資方資料與主系統資料是否有明確分區（不同資料表或 DB）。

API 權限是否正確實作，例如 /admin/*、/partner/*。

敏感資料（API Key、DB 密碼）是否在 .env 管理。

登入驗證是否採用 JWT / Session，token 是否有過期機制。

👉 輸出格式：

健康分數：x/10
安全疑慮：
-
建議：
-

🧠 五、整體健康度（程式與架構層面）

請檢查：

專案檔案結構是否清楚（modules / components / api）。

命名一致性與可維護性。

錯誤處理是否完善（try/catch、log）。

註解與文件完整度。

套件版本與相依衝突風險。

👉 輸出格式：

健康分數：x/10
潛在風險：
-
改善方向：
-

📋 最後請加上總結：

各模組健康平均分數

最優先需改善的三個面向

總體風險等級（低／中／高）

簡要建議下一步（例如：「建議優先優化權限分層與報表效能」）